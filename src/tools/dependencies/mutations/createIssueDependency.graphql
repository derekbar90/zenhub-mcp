mutation createIssueDependency($input: CreateIssueDependencyInput!) {
  createIssueDependency(input: $input) {
    issueDependency {
      id
      blockingIssue {
        id
        title
      }
      blockedIssue {
        id
        title
      }
    }
  }
}