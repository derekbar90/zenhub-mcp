query getWorkspaceLabels($workspaceId: ID!) {
  workspace(id: $workspaceId) {
    id
    name
    zenhubLabels {
      nodes {
        id
        name
        color
        description
      }
    }
  }
}