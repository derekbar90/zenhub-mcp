# Environment variable examples
# GitHub Personal Access Token (PAT) used by tools that call the GitHub REST API.
# Generate one at https://github.com/settings/tokens with appropriate scopes (e.g., repo).
GITHUB_PAT= 

# ZenHub API key used to authenticate with ZenHub GraphQL API
ZENHUB_API_KEY=

# Optional: Custom system prompt instructions for the ZenHub MCP server.
# If set, this text will fully replace the default instructions passed to the model.
# Use \n for newlines or a multiline value depending on your shell/file.
ZENHUB_MCP_CUSTOM_INSTRUCTIONS= 